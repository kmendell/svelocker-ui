import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
	// TODO: Add any server-side logic needed for login page
	return {
		// Add any data needed by the login page
	};
};
